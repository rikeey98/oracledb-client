[project]
name = "oracledb-client"
version = "0.1.0"
description = "Simple Oracle DB client using YAML config"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "oracledb",
    "PyYAML",
]
